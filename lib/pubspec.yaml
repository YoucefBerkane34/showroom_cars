name: showroom_cars
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Database and File Paths
  sqflite_common_ffi: ^2.3.3
  path: ^1.9.0

  # State Management and Formatting
  provider: ^6.1.2
  intl: ^0.19.0

  # PDF Generation and Printing
  pdf: ^3.11.1
  printing: ^5.13.2

  # Utilities and UI Charts
  file_picker: ^8.0.3
  shared_preferences: ^2.2.3
  fl_chart: ^0.68.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
