import 'package:flutter/material.dart';

class AppColors {
  // Primary brand color (deep blue like a premium showroom)
  static const Color primary = Color(0xFF1A237E);
  static const Color primaryLight = Color(0xFF3949AB);
  static const Color primaryDark = Color(0xFF0D1453);
  static const Color accent = Color(0xFFFF6F00);

  // Status colors
  static const Color available = Color(0xFF2E7D32);
  static const Color reserved = Color(0xFFE65100);
  static const Color sold = Color(0xFF1565C0);
  static const Color preparation = Color(0xFF6A1B9A);

  // Background
  static const Color background = Color(0xFFF5F6FA);
  static const Color surface = Colors.white;
  static const Color sidebarBg = Color(0xFF1A237E);
  static const Color sidebarText = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF1A1A2E);
  static const Color textSecondary = Color(0xFF6B7280);

  // Card shadow
  static const Color shadow = Color(0x1A000000);
}
